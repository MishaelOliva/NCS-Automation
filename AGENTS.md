# AGENTS.md

## Baseline
- Do not assume framework, language, layout, build, or test tooling until the project defines them.
- Do not depend on personal skill files, home-directory conventions, or user-local standards.
- Use official framework and language documentation as the primary source of truth.
- Prefer industry-standard patterns such as Clean Architecture and SOLID when they fit the project scope.

## Execution Rules
- Keep `max_parallel_subagents` at `2`.
- Detect the host hardware before tuning thread budgets.
- Reserve enough logical threads for the IDE and OS without hardcoding a machine-specific profile.
- Use at most one shadow worktree at a time.
- Use a shadow worktree for experimental features when the repository supports git worktrees.
- Prefer short burst tasks over long-lived loops.
- Prefer streamed, concise logs over raw terminal dumps.

## Hardware Rules
- Auto-detect CPU, GPU, and system RAM on the current host before deciding resource budgets.
- Keep headroom for the IDE and OS.
- Keep local code embeddings `4-bit` quantized.
- If VRAM exceeds `85%`, move embedding inference to CPU.

## MCP Rules
- Use `streamable-http` for MCP remotes.
- Prefer `mcp-schema.json` and `.mcp/config.json` before external MCP discovery.
- Keep `.mcp/server.json` synced with the active endpoint.
- Use `bootstrap.ps1` when Node.js/npm are missing so the protocol can install its own prerequisites before `npm install`.
- Use MCP-backed tools for visual navigation, isolated test execution, and deep debugging when available.
- Prefer MCP `workspace_context` and streamed logs over raw directory dumps for startup context.
- If a page is stuck, loaded but unresponsive, or visually wrong, inspect audit diagnostics first.

## Retro Rule
After each major task, append:
- What changed.
- What bottlenecks appeared.
- What to simplify or split next time.

## Retro
- What changed: upgraded the MCP SDK baseline, isolated bridge sessions per client, added startup doctor and endpoint sync scripts, exposed bounded MCP test execution, added a compact workspace context tool, and added single-shadow-worktree support.
- What bottlenecks appeared: port `7331` can already be occupied, browser-backed audit still depends on a local Chromium-family install, and shadow worktrees only apply when the target project is a git repository.
- What to simplify or split next time: keep browser control minimal unless screenshot plus test execution stops being sufficient.
- What changed: merged the protocols scaffold into the `YONDU` root, added root-level MCP bridge scripts and schema, and aligned the hardware profile to this machine.
- What bottlenecks appeared: `node` and `npm` were not available on the current shell `PATH`, so the MCP scripts could not be executed here.
- What to simplify or split next time: keep the nested bundle only as an archive once the root scaffold is established, and verify Node tooling availability before promising a runnable check.
- What changed: replaced the fixed hardware profile with runtime hardware detection so the protocol adapts on each host.
- What bottlenecks appeared: hardware detection still depends on PowerShell/WMI availability on Windows hosts.
- What to simplify or split next time: keep the manifest schema generic and let the doctor publish the live machine profile instead of hardcoding spec tables.
- What changed: added total system RAM to the runtime hardware profile so the bundle can adapt to low-memory and high-memory machines alike.
- What bottlenecks appeared: the RAM readout now depends on the same Windows detection path as CPU and GPU, so non-Windows support would need a separate fallback.
- What to simplify or split next time: keep the hardware contract small, but include the minimum fields needed for automatic budget decisions.
- What changed: added a bootstrap script that can install Node.js LTS and then run `npm install` before the bridge checks.
- What bottlenecks appeared: automatic prerequisite installation still depends on `winget` or `choco` being available on Windows.
- What to simplify or split next time: keep the bootstrap path explicit in the docs so transferred bundles do not stall at missing toolchain prompts.
- What changed: corrected the NCS form signature sections to follow the workbook geometry, switched editable signer cells to multiline fields so names and titles wrap instead of clipping, and overrode the legacy print grids that were still breaking the accountability and test-device layouts.
- What bottlenecks appeared: the redesign had to coexist with older accountability and test-device CSS rules, so print and screen each needed targeted overrides instead of one shared grid change.
- What to simplify or split next time: isolate workbook-specific signature and print styles into a dedicated stylesheet block so future branding refreshes do not have to fight the legacy form selectors.
- What changed: locked the accountability and test-device signature cards to fixed row heights and gave the pickup row its own workbook-style cell treatment so all three cards align consistently on screen and in print.
- What bottlenecks appeared: content-driven row sizing from wrapped titles and multiline signer fields was still stretching the approved-by card even after the initial grid fixes.
- What to simplify or split next time: define signature panels as explicit row templates from the start instead of letting shared utility classes size critical form cells.
- What changed: restored the workbook-style assignee caption rows by showing fallback text when the assignee fields are empty, so the left signature card no longer renders as broken blank cells before lookup data is filled.
- What bottlenecks appeared: the assignee card looked structurally wrong even after the alignment fixes because the workbook template uses visible fallback captions, not empty rows.
- What to simplify or split next time: treat workbook placeholder text as part of the template contract and encode it directly with the signature component instead of relying on empty editable fields.
- What changed: restored the return-and-sanitation pairing for the sanitization workflow so the return form and sanitation form render together on screen and in print, matching the workbook behavior.
- What bottlenecks appeared: the paired workflow logic was still present, but the redesign had hidden the return template from the active sanitization path and from print visibility.
- What to simplify or split next time: keep paired workbook workflows behind one shared mode definition so layout updates do not accidentally hide one half of the pair.
- What changed: replaced the old legacy return form with the new workbook return layout, including the revised info grid, six-column asset condition table, affirmation band, and two-panel signature section.
- What bottlenecks appeared: the old return implementation reused a different table and footer structure, so the workbook redesign required touching the live template, row renderer, print builder, and return-specific styling together.
- What to simplify or split next time: define each workbook form variant as its own dedicated template module so future sheet swaps do not require patching both the interactive HTML and the print generator in parallel.
- What changed: promoted the Codex MCP scaffold into the `C:\NCS` root, installed the root dependencies, aligned the root docs and startup instructions, and verified `doctor`, `probe`, and the existing Node test suite from the active root setup.
- What bottlenecks appeared: the repository is not a git workspace here, so diff and shadow-worktree protocol features remain unavailable until the project is initialized as a git repo.
- What to simplify or split next time: keep the reusable scaffold archive separate from the active root files so setup only needs one promotion step and no doc cleanup.
- What changed: restyled the accountability, test-device accountability, and sanitization templates to match the workbook tabs at the root visual level, switched the in-app logo/branding to NCS, aligned the print builders with the new workbook-based layouts, and kept the existing autofill/test coverage green.
- What bottlenecks appeared: the workbook layout and the existing DOM/feature set diverged on test-device remarks/signature fields and the legacy return/sanitization pairing, so some internal fields had to stay hidden to preserve behavior while the visible form matched the workbook.
- What to simplify or split next time: separate printable template markup from non-print operational fields earlier so workbook-accurate redesigns do not have to share table cells with hidden compatibility inputs.
- What changed: fixed CSS design conflicts against the four active workbook forms by preventing the test-device form from leaking into unrelated print views, restoring workbook-specific return/sanitation print column widths, and normalizing test-device table columns to a 100% layout.
- What bottlenecks appeared: the stylesheet still contains older generic print rules and global formatting drift, so targeted fixes needed higher selector care and Biome check reports broad pre-existing format churn.
- What to simplify or split next time: move workbook-form print overrides into one dedicated NCS print block so legacy table rules cannot outrank the active workbook layouts.
- What changed: rebuilt the Asset Condition and Asset Condition Remarks cells for the return and sanitation forms to follow the workbook's two-row merged blocks on screen and in print.
- What bottlenecks appeared: the original implementation used one five-row rowspan for the whole condition area, which hid workbook row lines and forced awkward stacked text in both the live form and generated print pages.
- What to simplify or split next time: centralize workbook table-cell geometry in a small template helper so static HTML, dynamic rows, and print markup cannot drift apart.
- What changed: replaced the visible FALSE markers in return and sanitation Asset Condition cells with empty checkbox squares across static, dynamic, and print markup.
- What bottlenecks appeared: the condition marker text was duplicated across the HTML template, dynamic row renderer, and print builder.
- What to simplify or split next time: reuse one condition-option markup helper for screen and print so workbook checkbox changes only need one edit.
- What changed: removed the extra bottom row from the return and sanitation asset tables, moved the damage instruction to the upper remarks block, and changed its wording to "Specify the damage below if applicable."
- What bottlenecks appeared: the four-row workbook shape had to be patched in static HTML, dynamic rendering, and print rendering because each path owned its own row padding.
- What to simplify or split next time: keep the return/sanitation table row count and condition remarks placement in one shared form-layout constant.
- What changed: unified the print fit scale for all active form templates at 99% through a shared print CSS variable.
- What bottlenecks appeared: previous print scale values were scattered across accountability, return/sanitation, and test-device overrides, so the same print behavior required touching multiple selectors.
- What to simplify or split next time: keep global print sizing in one top-level print token and reserve per-form rules for geometry only.
- What changed: compacted the IT Asset Sanitation generated print page so the final-page affirmation and all five signature panels fit within A4 landscape at the 99% print scale.
- What bottlenecks appeared: the sanitation overflow page shares the generic return/sanitation print builder, but its five-panel signature footer has a much taller vertical footprint than the return form.
- What to simplify or split next time: give sanitation final pages their own named print density token so footer height can be tuned without adding many selector-level overrides.
- What changed: rechecked the workbook return and sanitation sheet renders, restored workbook-style print width, divider bands, signature panel spacing, and zero page margins for the paired print path.
- What bottlenecks appeared: Chrome print pagination uses the CSS `@page` margin even when the UI says margins are none, so the sanitation bottom signature row needed both page-margin correction and small vertical tuning.
- What to simplify or split next time: define return and sanitation print geometry from workbook measurements in one shared constants block, with a separate sanitation footer height budget.
- What changed: relaxed the one-page asset accountability print layout for blank and single-asset states by increasing detail row height, authority spacing, and signature panel height.
- What bottlenecks appeared: the default visible accountability row is counted as `data-asset-count="0"` until real lookup data is present, so a single-asset selector alone did not affect the print preview.
- What to simplify or split next time: expose an explicit print density state for blank-template versus populated accountability forms instead of inferring from row count.
- What changed: tightened the blank and single-asset accountability print density so the authority block and signature footer fit on one A4 landscape page again.
- What bottlenecks appeared: Chrome pagination is sensitive to a few millimeters of accumulated row, authority, and signature height, so the earlier relaxed spacing pushed the footer to page two.
- What to simplify or split next time: keep a measured one-page height budget for each accountability density state instead of tuning individual rows independently.
- What changed: added an accountability-only print scale, tightened the single-asset rows further, and bumped the stylesheet cache keys so Chrome print preview reloads the updated print rules.
- What bottlenecks appeared: the live populated form was taller than the blank headless check, and the versioned CSS import could keep stale print styling in the browser preview.
- What to simplify or split next time: verify populated print states and cache-busted styles together whenever a print fix depends on live form data.
- What changed: tightened the dual-lookup header so its cards and autofill action fill the available NCS workspace width instead of leaving a large right-side dead zone; added responsive two-column and single-column fallbacks and bumped the stylesheet cache key.
- What bottlenecks appeared: the header and lookup panel are owned by separate legacy flex/grid rules, so the dead-space fix needed a high-specificity mode override while preserving the existing mobile and print contracts.
- What to simplify or split next time: give each lookup mode a shared layout token/component so wide, compact, and print geometry are defined once instead of layered across legacy selectors.
- What changed: added model-based accountability remarks autofill for all standard accountability modes, using box-and-charger wording for Apple models and bag-and-charger wording for Windows/non-Apple laptop models, while preserving manual remarks.
- What bottlenecks appeared: the existing shared remarks helper only updated return/sanitization fields and the main accountability remarks cell was populated separately during row rendering, so the new behavior had to be attached to both primary and additional accountability rows.
- What to simplify or split next time: keep model classification and asset-condition defaults in one shared asset metadata helper so future device-specific autofill rules do not need to be threaded through multiple render paths.
- What changed: converted the Test Device accountability Approved By name and role from static text into editable, autofilled textareas and restored their defaults during form reset.
- What bottlenecks appeared: the lightweight Node test DOM does not parse the live HTML template or dynamically rebuilt rows, so the regression check had to validate the source template markup instead of exercising browser DOM reset behavior.
- What to simplify or split next time: add a small HTML-aware fixture or browser smoke test for template-level editing behavior when future form changes depend on static markup plus runtime resets.
- What changed: converted Return and Sanitation signature names and roles to editable autofilled fields, connected the paired form autofill defaults, and carried edited values into generated print footers.
- What bottlenecks appeared: the same signature content existed in live HTML, runtime field setup, source lookup fallbacks, and print-only markup, so each editable field needed a stable ID and mirrored print snapshot value.
- What to simplify or split next time: define the return/sanitation signature roster once and render both live and print versions from that shared metadata instead of maintaining parallel field lists.
- What changed: added a Returned Only Return/Sanitation mode button that synchronizes both status fields to RETURNED and recognizes RETURNED when entered manually.
- What bottlenecks appeared: status-to-mode inference had a broad return fallback that would have classified RETURNED as purchased unless the new specific case ran first.
- What to simplify or split next time: keep status aliases and print mode definitions in one mapping so button labels, status values, inference, and filename suffixes cannot drift apart.
- What changed: split Test Device autofill validation into targeted messages for missing assignee, missing device lookup, and both fields missing.
- What bottlenecks appeared: the original validation combined two independent empty-state checks into one generic alert, so the UI could not tell the user which lookup needed attention.
- What to simplify or split next time: keep form validation messages in small pure helpers so each lookup workflow can be tested without invoking the full async search flow.
- What changed: reviewed and standardized user-facing error phrases across CSV upload, source readiness, accountability, return/sanitation, and Test Device search flows; made prompts consistently actionable and clarified missing-source and no-match wording.
- What bottlenecks appeared: error text was distributed across direct alerts, source configuration fallbacks, and resolver-returned messages, so consistency required reviewing both UI call sites and lower-level search errors.
- What to simplify or split next time: centralize user-facing validation and lookup messages by workflow so wording changes do not require scanning multiple modules.
- What changed: enlarged the Asset Accountability print fit from 92% to 110% so the one-page form uses nearly the full A4 landscape sheet without changing its workbook geometry.
- What bottlenecks appeared: the previous accountability-only scale was substantially smaller than the shared print scale, which created large symmetric side gaps and a deep unused area below the form.
- What to simplify or split next time: keep a measured A4 landscape fit target beside each form-specific print scale so later density adjustments preserve both one-page pagination and page utilization.
- What changed: added adaptive Asset Accountability pagination that keeps up to six ordinary laptop rows on the tuned one-page form, lowers capacity when wrapping risk accumulates, balances larger lists across continuation pages, repeats asset ranges and page numbers, and keeps authority/signatures on the final page only.
- What bottlenecks appeared: the accountability print rules are layered across legacy and NCS-specific media blocks, and the in-app browser blocks local file URLs, so Chromium PDF artifacts and Poppler renders were used to verify the one-page and two-page results.
- What to simplify or split next time: consolidate accountability page capacities, wrap-risk thresholds, and page geometry into one print-layout module with a maintained Chromium PDF smoke matrix.
- What changed: bottom-aligned the Print Form action with the dual-lookup Autofill action so compact desktop and tablet headers keep both primary buttons on the same visual baseline.
- What bottlenecks appeared: the lookup action was aligned to the bottom of its internal grid while the separate header action inherited center alignment from the taller header row.
- What to simplify or split next time: place related form actions in one shared action rail so responsive alignment does not depend on separate grid containers.
- What changed: removed blank trailing Asset Accountability sheets by giving generated pages unscaled A4-safe geometry, compacted the Sanitation print footer and table, and raised the paired Return/Sanitation one-page capacity to six assets so the workflow stays within two pages.
- What bottlenecks appeared: DOM page counters did not reveal Chromium's trailing blank sheets, and CSS zoom produced correct first pages but clipped later accountability headers, so fresh PDFs and Poppler renders were required across one-, two-, and three-page cases.
- What to simplify or split next time: keep a permanent browser-to-PDF smoke matrix with asserted PDF page counts for Test Device, paired Return/Sanitation, and multi-page Accountability scenarios.
- What changed: adjusted the sanitization print signature grids to use equal card widths, shared gaps, a centered second row, and a slightly more generous writing area; bumped the stylesheet cache key and added a CSS regression test.
- What bottlenecks appeared: the in-app browser blocks local file URLs in this session, so visual verification used the supplied preview/print renders plus source and targeted test checks; the existing full suite still has four unrelated Test Device failures.
- What to simplify or split next time: move sanitization print geometry into shared layout tokens and keep a permitted local print-render smoke check for signature-panel spacing.
- What changed: kept Asset Accountability signatures on the first print sheet, added print-only mirrors with auto-growing rows for long names and roles across every form, and made long position and description values wrap without clipping.
- What bottlenecks appeared: layered legacy print rules combined a page-level authority spacer with fixed signature flex-bases, so the fix needed a final high-specificity print contract and fresh cache keys.
- What to simplify or split next time: centralize all printable form geometry and signature rows behind shared layout tokens with measured one-page height budgets.
- What changed: corrected the sanitization print grid regression by restoring preview-like spacing, centering the reduced-width bottom signature table with auto margins, matching the preview grid gaps, and restoring the larger blank signing cells; bumped the stylesheet cache key again.
- What bottlenecks appeared: the print preview screenshot made the missing auto margins obvious, but local file navigation remains blocked in the in-app browser, so final verification used the supplied visual evidence and focused source tests.
- What to simplify or split next time: keep preview and print signature geometry in one shared contract so a print-only compression pass cannot change table placement independently.
- What changed: kept the corrected sanitization signature placement while reducing only the bottom-row print margin to 1.6rem, added page-fragment avoidance to both signature grids, bumped the cache key, and preserved the larger signing cells so the form stays on one page.
- What bottlenecks appeared: the third-page leak was caused by Chromium moving the intact bottom grid when the final few print pixels exceeded the page budget; source tests can validate the CSS contract but cannot assert Chromium page count while local file preview is blocked.
- What to simplify or split next time: add a one-asset sanitization PDF page-count smoke case alongside the existing print CSS regression test.
- What changed: removed the white logo badge from the sidebar brand lockup and rendered the existing transparent NCS mark as a white-and-cyan wordmark directly on the navy panel.
- What bottlenecks appeared: the source PNG includes large transparent margins and dark brand lettering intended for light backgrounds, so CSS had to use its alpha as a mask and recolor the mark without affecting printed forms.
- What to simplify or split next time: keep light-background and dark-background logo variants as explicit shared brand assets instead of recoloring one raster at the component level.
- What changed: added adaptive return/sanitation print density for long employee and signer names, shrinking the print scale and tightening fixed signature rows only when needed so the bottom signature row stays on the final sheet; added focused regression coverage and bumped the stylesheet cache key.
- What bottlenecks appeared: the in-app browser can exercise the live form and print trigger but does not expose a reliable PDF page-count assertion, so pagination protection is covered by the shared density helper and print CSS contract while the existing four unrelated Test Device harness failures remain.
- What to simplify or split next time: add a permanent Chromium PDF smoke matrix for long-name return/sanitation cases so adaptive print density can be verified against actual page counts and clipping, not only source-level layout rules.
- What changed: restored the transparent NCS sidebar mark with a visible fallback, preserved the cyan/white mask styling, and bumped the experience stylesheet cache key.
- What bottlenecks appeared: the local in-app browser blocks file URLs in this session, so rendered visual verification could not be completed there; the existing PNG was transparent but its masked pseudo-element was not reliably visible.
- What to simplify or split next time: keep a dedicated light-on-dark logo asset or SVG variant so the sidebar does not depend on runtime masking/recoloring for brand visibility.
- What changed: replaced the sidebar logo asset with the user-supplied transparent NCS artwork and bumped the stylesheet cache key again so the new proportions load immediately.
- What bottlenecks appeared: the supplied PNG is transparent and valid, but the local in-app browser still blocks file URLs, so final visual confirmation remains unavailable in this session.
- What to simplify or split next time: keep the approved transparent brand artwork as a named light-on-dark asset so future logo swaps do not require mask fallbacks.
- What changed: kept four Test Device accountability rows and their authority/signature footer on one A4 landscape print page, added print-only mirrors so long laptop descriptions wrap fully, and gave compact Accountability/Return/Sanitation signer rows enough height for wrapped names and roles; bumped the stylesheet cache key.
- What bottlenecks appeared: the print stylesheet has several legacy and late density blocks, and browser pagination treated fixed signature grids differently from their measured DOM rectangles, so final verification required headless Brave PDF generation plus Poppler renders for every form.
- What to simplify or split next time: consolidate printable value mirrors, signature row budgets, and per-form density overrides into one print-layout module with a permanent four-template PDF smoke matrix.
- What changed: audited the four supplied CSVs against every autofill lookup path, corrected composite-header precedence, added ITSM `xr`, New Hire business-unit, deployed/issued-date, and cost-center mappings, fixed serial/IMEI-to-asset resolution, preferred canonical duplicate TD tags, preserved space-separated IMEIs, honored manual source roles, and rejected duplicate device identifiers; expanded regression coverage to 41 passing tests.
- What bottlenecks appeared: the CSVs contain historical/current TD tag conflicts, one duplicate master serial, malformed Test Device assignee identifiers, and four suspicious New Hire employee IDs, so source data quality remains a separate operational risk even after the lookup mechanics are corrected.
- What to simplify or split next time: add a source-quality warning layer for malformed identifiers and duplicate asset keys, and keep an exact-file smoke matrix for each live CSV export alongside the unit suite.
- What changed: softened the sidebar logo treatment into a white-to-light-blue gradient that follows the page's cyan accent and bumped the cache key.
- What bottlenecks appeared: the local in-app browser continues to block file URLs, so the gradient was verified through source checks and the test suite rather than a live screenshot.
- What to simplify or split next time: expose the logo gradient as a theme token so brand contrast can be tuned centrally with the rest of the visual system.
- What changed: restored the supplied logo's original navy/cyan colors, placed the white-to-blue gradient behind it as a compact rounded backdrop, and removed the temporary recolored sidebar asset.
- What bottlenecks appeared: the attached screenshots and source PNGs use different clipboard paths, so the latest path had to be validated as the logo source before replacing the project asset.
- What to simplify or split next time: keep one approved logo source path and a separate backdrop token so future brand-color changes cannot recolor the mark itself.
- What changed: moved the white-to-blue gradient from the logo badge to the full top branding container, kept the supplied navy/cyan logo transparent, adjusted header text for contrast, and bumped the cache key.
- What bottlenecks appeared: the visual intent depended on the gradient's container boundary rather than its color values, so the logo and header needed separate background ownership.
- What to simplify or split next time: model the sidebar brand header as one self-contained component with logo, background, and contrast tokens instead of styling the mark independently.
- What changed: shifted the branding container from a near-white card to a blue/cyan gradient with a navy fade that blends into the surrounding deep-blue sidebar, while preserving the approved logo colors.
- What bottlenecks appeared: the previous light gradient had the correct boundary but too much luminance contrast against the panel, so color balance—not structure—was the remaining issue.
- What to simplify or split next time: define the brand-header gradient stops from the sidebar palette tokens so container contrast stays coordinated with theme changes.
- What changed: expanded one-page Asset Accountability print output to use the available sheet height, added a print mirror for long Position values, and removed fixed-height clipping from model/description rows; bumped the print stylesheet cache key.
- What bottlenecks appeared: late layered print-density rules overrode earlier height and margin changes, so the final fit and wrapping contract had to be placed at the end of the print stylesheet.
- What to simplify or split next time: consolidate accountability print density, page height, and value-mirror rules into one final layout module instead of stacking many media overrides.
- What changed: made CSV uploads replace existing recognized sources by detected role, remove prior duplicates in that role, preserve generic files unless their canonical filename matches, and updated the upload tutorial/cache key; added role-replacement regression coverage.
- What bottlenecks appeared: canonical filename replacement already existed, but differently named exports could occupy duplicate role slots, so replacement had to account for both role and filename matches while preserving manual role assignments.
- What to simplify or split next time: centralize source-slot ownership and upload reconciliation rules so UI cards, lookup deduplication, and storage updates share one source-role contract.
- What changed: reduced Asset Accountability compact print scale and signature blank row heights, and bumped stylesheet cache keys so the tighter one-page print fit is delivered on refreshed print previews.
- What bottlenecks appeared: the remaining leak came from dense compact signatures and aggressive scale on accountability rows where long names/descriptions still pushed content beyond A4-height.
- What to simplify or split next time: tie print height safety to an explicit density tier (normal/compact/tight) with measured fallback checks so scale/spacing can be tuned without repeated override edits.
- What changed: enlarged compact Accountability signature blank rows to 4rem and shifted compact table blocks upward to reclaim vertical space without changing row geometry.
- What bottlenecks appeared: spacing improvements that preserve one-page fit can be fragile because final-table alignment interacts with legacy compact and overflow-page selectors.
- What to simplify or split next time: introduce a single compact-signature-and-table offset token and remove duplicated selector sets that currently require parallel overrides.
- What changed: aligned Asset Accountability print signatures with the Test Device print flow by removing the bottom-push auto margin, matching the compact signature-grid padding, preserving page-fragment avoidance, and adding a focused regression check; bumped the stylesheet cache keys.
- What bottlenecks appeared: legacy flex sizing and late print overrides made the accountability signature position depend on an auto margin, and the in-app browser blocked localhost so headless Brave was needed for visual verification.
- What to simplify or split next time: share one print signature layout token between Accountability and Test Device instead of maintaining repeated form-specific overrides.
- What changed: made the responsive preview fluid below 760px and explicitly stacked the Test Device signature cards below 960px, so all lookup modes and printable previews stay inside narrow windows; bumped the experience stylesheet cache key and added responsive CSS coverage.
- What bottlenecks appeared: a more-specific Test Device desktop selector overrode the shared mobile stack, while the old 52rem preview minimum caused silent clipping because the app hides page-level horizontal overflow.
- What to simplify or split next time: centralize responsive preview width and breakpoint tokens so form-specific desktop geometry cannot override the mobile contract.
- What changed: replaced the wide dual-lookup three-column header with two equal lookup cards and a dedicated action row across Accountability replacement, Test Device, and Test Device return/sanitation modes; bumped both stylesheet cache keys and added regression coverage.
- What bottlenecks appeared: the earlier responsive fix only changed narrow preview breakpoints, so the desktop action track continued squeezing both lookup inputs and looked unchanged after refresh.
- What to simplify or split next time: keep the dual-lookup grid and action rail in one shared desktop contract, with only the single-column mobile override defined separately.
- What changed: made Accountability and Test Device Accountability print signatures use the same three equal 24.5% cards and preview-style spacing, removed every legacy 29%/36%/29% grid, hardened the final responsive dual-lookup contract, and bumped the nested print and experience cache keys.
- What bottlenecks appeared: the earlier signature fix changed only vertical flow, leaving the wider center card intact, while the unchanged nested print stylesheet URL made that omission look like a refresh problem.
- What to simplify or split next time: define one shared signature-grid geometry token for preview and print, and generate all stylesheet cache versions from one build identifier.
- What changed: populated the Test Device Accountability assignee role from the employee Position field, labeled the editable signature row correctly, added four print-fit tiers for long signer roles, and fixed the role row height so text scaling cannot increase the print page count.
- What bottlenecks appeared: the assignee role control retained the legacy `f_tdaCostCenter` identifier and was explicitly cleared during employee autofill even though the Position value was already available in the same payload.
- What to simplify or split next time: rename legacy signer controls around their visible purpose and render employee signature name/role from one shared binding instead of assigning the detail and signature fields independently.
- What changed: replaced the legacy purple Form Comfort sample with an NCS-branded accountability preview using the approved logo, navy title and detail bands, cyan accents, NCS labels, and a coordinated dark-mode variant; refreshed the settings stylesheet cache keys.
- What bottlenecks appeared: the comfort overlay and dark-mode preview intentionally alter paper color, so the NCS identity had to remain fixed while those state-driven layers continued to demonstrate their effects.
- What to simplify or split next time: promote the workbook navy, sky, ink, and border colors to shared global form-preview tokens so the real forms and settings sample cannot drift into separate palettes.
- What changed: added a Return/Sanitation-only Copy row action beside Data sources that copies one nine-column Google Sheets TSV row per returned laptop, monitor, or test device, including the resignation marker, two manual-entry gaps, and status-based remarks.
- What bottlenecks appeared: paired Return and Sanitation forms duplicate their editable values, file-based clipboard behavior needed a browser fallback, and the repository's legacy `with`-scope modules prevent Biome from parsing targeted JavaScript checks.
- What to simplify or split next time: centralize the paired-form field snapshot and spreadsheet column mapping in one reusable serializer, then keep a localhost clipboard smoke test alongside the Node regression suite.
- What changed: replaced the single-line Return, Sanitation, and Test Device Accountability description controls with auto-growing, screen-wrapping fields across static and generated rows; verified Asset Accountability already wraps safely and left the print renderer and stylesheet unchanged.
- What bottlenecks appeared: the shared Test Device textarea styling imposes a large fixed minimum height, so description fields needed a dedicated screen-only class to expand from one row without altering the approved print geometry.
- What to simplify or split next time: render long editable asset descriptions through one shared field component so static templates, extra rows, preview behavior, and print mirrors cannot drift independently.
- What changed: restored normal print flow between the Asset Accountability title, employee information table, asset table, and authority section; removed the compact double-offset overlap, preserved the one-page height budget, refreshed stylesheet cache keys, and added regression coverage.
- What bottlenecks appeared: one late compact-print rule applied both a negative margin and a negative relative offset to three consecutive blocks, visually hiding Employee Number and Position while source-level row sizing still appeared correct.
- What to simplify or split next time: replace cumulative print offsets with named density spacing tokens and keep title-to-table separation as an explicit invariant in the print layout contract.
- What changed: completed a read-only performance audit of startup, CSV parsing and lookup, DOM and print preparation, storage, and static assets; established a green 54-test baseline and measured the dominant large-row lookup costs without changing the app.
- What bottlenecks appeared: each lookup repeatedly normalizes row headers and scans full sources, uploads discard useful parse caches and rebuild source cards twice, and the direct-file delivery path eagerly loads oversized raster, font, CSS, tutorial, and print assets.
- What to simplify or split next time: implement source schema accessors and exact-match indexes first, preserve current scoring and ambiguity behavior with parity tests, then isolate cache, print-dirty, and delivery optimizations into separately verified changes.
- What changed: added shared Padua and Mendez signer rosters to every applicable form, seeded the requested alternatives, added persistent add/delete controls, protected required defaults, kept employee/returnee/assignee fields unmanaged, and preserved selected values in print snapshots.
- What bottlenecks appeared: the signer values are reused by live textareas, autofill defaults, generated print pages, and paired Return/Sanitation forms, while signature cards sit near the bottom of overflow-hidden paper previews.
- What to simplify or split next time: render all staff signature panels from one roster-aware component and keep signer identity, role, picker behavior, and print serialization in a single schema.
- What changed: superseded protected signer defaults with persisted, deletable defaults for every staff roster; kept each signer textarea directly editable beside a compact roster button; added set-default controls; extended roster management to technical support and InfoSec; and routed autofill, reset, and print fallbacks through the saved defaults.
- What bottlenecks appeared: editable signer values and saved roster entries are intentionally separate states, so direct manual text had to remain printable without silently becoming a saved/default entry, while deleting the final roster entry needed a safe blank fallback.
- What to simplify or split next time: define staff signer rosters, role labels, live field bindings, and print fallbacks in one declarative roster schema so new signature roles do not require parallel HTML and autofill edits.
- What changed: generalized asset-tag autofill to remove leading YONDU, YODNU, YONDD, and YONUD prefixes across Accountability, Test Devices, Return/Sanitation, print, and Google Sheets copy; preserved raw and cleaned lookup aliases; and added safe Ctrl+C and Ctrl+Shift+C Copy row shortcuts.
- What bottlenecks appeared: asset tags share generic identifier plumbing with serials, IMEIs, and employee IDs, while Test Device formatting normally adds a TD prefix and browser copy shortcuts can conflict with native text selection behavior.
- What to simplify or split next time: centralize field-aware identifier canonicalization and search aliases in one asset-tag contract, and keep application shortcuts in a shared registry with explicit native-copy exclusions.
- What changed: replaced the 17-step autoplay tutorial with a nine-step user-paced walkthrough, corrected the source and lookup guidance to match current behavior, added Back/Next/Finish controls and accessible progress, made exit always available, and prioritized natural speech voices with conversational narration.
- What bottlenecks appeared: the app's asset-loading path changed to direct optimized styles and a lazy tutorial loader while the tutorial work was in progress, and the lightweight test harness still does not execute tutorial interactions directly.
- What to simplify or split next time: move tutorial steps and narration into a small declarative data module, then add a dedicated DOM fixture for navigation, focus, progress, and state-restoration tests.
- What changed: raised the practical one-page Asset Accountability limit to six realistic laptop rows, added a high-wrap tier that deliberately paginates long descriptions before Chromium can split the form, balanced continuation-page allocations, and extended the real-PDF smoke test across 1-20 realistic and 1-10 high-wrap assets.
- What bottlenecks appeared: the prior tests used very short model names, the remainder allocator could repeatedly favor one page, and logical page counts alone did not reveal that four long rows could become two physical sheets.
- What to simplify or split next time: keep print-capacity tiers and wrap thresholds in one measured layout contract, and retain the physical PDF boundary matrix whenever form geometry or browser print rules change.
- What changed: audited every active form through real A4-landscape PDFs, fixed Return-to-Sanitation continuation boundaries, kept six dense laptop or Test Device return rows to two sheets, replaced clipped information and remarks controls with fitted print mirrors, and made long Accountability signer rows size safely without overlap.
- What bottlenecks appeared: Chromium paginates CSS-zoomed form content from its unscaled layout height, nested remarks controls bypassed direct-child print hiding, and fixed signature flex bases let mirror text extend through row borders even when DOM overflow checks appeared clean.
- What to simplify or split next time: consolidate live controls, print mirrors, dense-state spacing, and template page breaks into one print-layout component and keep the nine-scenario rendered-PDF matrix as a permanent regression suite.
- What changed: indexed the autofill lookup hot paths, made CSV parsing and source diagnostics incremental, batched DOM work, deferred expensive print preparation, lazy-loaded tutorial and signer options, reduced static assets, and added repeatable performance and physical-PDF regression checks.
- What bottlenecks appeared: wide CSV header normalization dominated cold lookup latency, layered print CSS required fresh Chromium and Poppler verification, and the legacy `with (scope)` construct keeps the repository-wide Biome check from parsing the existing architecture.
- What to simplify or split next time: centralize source-index invalidation and print-mirror geometry, retain the benchmark and PDF boundary matrices, and migrate away from `with (scope)` before enforcing Biome across the whole repository.
- What changed: preserved the New Hire Attendance second-row `POSITION` label as a canonical field when column I is prefixed by the export's first header row, added regression coverage for the live April 2026 layout, and versioned the source-management script URL so refreshed browsers load the parser update.
- What bottlenecks appeared: the existing composite-header aliases could resolve the position in isolation, but they made the live field depend on prefix heuristics even though the source label was explicitly `POSITION`; the unchanged script URL also kept a normal browser refresh on the old parser.
- What to simplify or split next time: keep source-specific canonical column bindings beside the parser and manage JavaScript cache versions from one build identifier, so important form fixes cannot be masked by stale assets.
- What changed: changed the ninth Google Sheets column produced by Return/Sanitation Copy row to title-case each status segment (for example, `Returned/Resign`), while leaving the visible form status unchanged; versioned the sheet-copy script URL and updated the row-output regression checks.
- What bottlenecks appeared: the copy flow correctly derived the resignation marker from the uppercase status, but then reused that same uppercase display value in the final pasted cell; the initial lowercase request required one format refinement.
- What to simplify or split next time: encode each spreadsheet column's formatting contract beside its value source so display-only casing cannot leak into integration exports.
- What changed: moved Asset Accountability signature cards into the lower-middle of the Authority to Deduct area for blank, one-asset, and two-asset print layouts; versioned the print stylesheet and added layout assertions without changing dense multi-asset geometry.
- What bottlenecks appeared: a late compact-flow rule intentionally pulled the cards directly below the authority text to protect pagination, so the visual adjustment had to be limited to pages with available vertical space.
- What to simplify or split next time: define the Authority section's card-placement tier alongside the existing asset-count density contract so visual whitespace and pagination are tuned together.
- What changed: added and then widened equal 1.25rem inner gutters on the Asset Accountability signature grid so the Assignee and Approved By cards have clear space from the Authority section borders; refreshed the print stylesheet and verified the rendered A4 page matrix.
- What bottlenecks appeared: the signature grid had only inherited sub-pixel side padding, so its outer cards looked nearly flush despite the intended three-card spacing; the first added gutter needed a modest visual increase.
- What to simplify or split next time: keep horizontal card gutters as an explicit print-grid token, separate from vertical density and signature-position rules.
- What changed: gave every dynamically added Return/Sanitation asset its own editable Remarks combobox and populated it with the active laptop or Test Device options, so per-device remarks carry through to the existing print serializer.
- What bottlenecks appeared: only the primary Remarks field had a fixed ID and dropdown container; added devices rendered a bare textarea, so option initialization never reached them.
- What to simplify or split next time: render primary and added asset Remarks fields through one shared form-field component from the start, with stable per-row IDs and option bindings.
- What changed: added a persistent triangle-warning banner that appears after any successful Autofill when core employee or asset fields are still blank or `N/A`, with a direct prompt to upload the latest corrected CSV; applied the check to every Accountability and Return/Sanitation autofill route, including Test Devices.
- What bottlenecks appeared: the app has several independent success paths for standard search, replacement lookup, and Test Device lookup, and replacement's independent employee/asset inputs need different completeness expectations.
- What to simplify or split next time: route all autofill completions through one result presenter that owns success, warning, source-summary, and field-completeness messaging.
